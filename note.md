for range(begin,end) 实际上是左开右闭的
pycharm中灰色为无用处等
list可以使用反向索引 结尾为-1 向前为-2等
python中常见sum() len()
for循环中in后需要接可遍历容器 整数用range
python中可以直接判断元素是否in容器中
简单形式的逻辑列表建立可以使用列表推导式：[要插入的值for i in 序列if条件]
str,tuple类型有不可变性
输出可指定保留小数{num:.1f}指保留一位小数
set之间的精简运算可用& | -等求交并差集
使用match on时 注意缺省 _
